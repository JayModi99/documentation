---
title: "Scanner flows"
description: "QR / tag scanning with geolocation."
---

# Scanner flows

Uses authenticated `POST /api/v1/scans/resolve` and mirrors asset eligibility for intents (checkout, check-in, etc.). Public URL scans hit `/api/v1/public/scans`.
