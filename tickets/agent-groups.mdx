---
title: "Agent groups"
description: "TicketAgentGroup membership used as category defaults."
---

# Agent groups

API: `/api/v1/TicketAgentGroup`  
Assignment on a ticket is still a **user** FK — groups are master data + `TicketCategory.DefaultGroupId` for routing UX.
