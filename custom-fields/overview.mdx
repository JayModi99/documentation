---
title: "Custom fields overview"
description: "Extensible fields attached to module forms across AssetMate Pro."
---

# Custom fields overview

Definitions are company-scoped. Values attach to entity instances by `(FormName, EntityId)`.

API: `/api/v1/CustomField` including `for-form`, module summaries, usage report, sort-order, export.
